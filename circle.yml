machine:
  node:
    version: 8.6.0
dependencies:
  pre:
    - npm install -g istanbul yarn codeclimate-test-reporter
  override:
    - yarn install
test:
  post:
    - codeclimate-test-reporter < coverage/lcov.info
general:
  artifacts:
    - ./coverage
